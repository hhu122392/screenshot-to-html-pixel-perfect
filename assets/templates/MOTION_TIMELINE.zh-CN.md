# 动效时间线

## 交互锚点

- 场景：
- 触发器：
- 相对时间零点：
- 片段开始 / 结束：
- 声明帧数 / 解码帧数：

## 主体轨道

逐个记录边界、变换中心、位置、缩放、旋转、透明度、层级、延迟、时长、缓动证据、循环规则和结束状态。

## 审计时钟

- 已实现 `window.__setAuditTime(milliseconds)`：
- 使用的源时间点：
- 候选帧数：

## 失败帧

列出每个平均绝对通道差 `>= 2` 的帧和修正动作。

