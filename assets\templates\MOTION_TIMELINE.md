# Motion Timeline

## Interaction anchor

- Scenario:
- Trigger:
- Relative time zero:
- Clip start / end:
- Declared / decoded frames:

## Actor tracks

For each actor, record bounds, transform origin, position, scale, rotation, opacity, z-order, delay, duration, easing evidence, loop rule, and terminal state.

## Audit clock

- `window.__setAuditTime(milliseconds)` implemented:
- Source timestamps used:
- Candidate frame count:

## Frame failures

List every frame with mean absolute channel difference `>= 2` and the corrective action.

